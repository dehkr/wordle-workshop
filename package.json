{
  "name": "wordle-workshop",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node ./scripts/build-assets.mjs",
    "watch": "node ./scripts/build-assets.mjs --watch",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types",
    "check": "biome check",
    "check-summary": "biome check --reporter=summary"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.5",
    "esbuild": "0.25.4",
    "wrangler": "^4.49.0"
  },
  "dependencies": {
    "alpinejs": "^3.15.2",
    "hono": "^4.10.6"
  }
}
