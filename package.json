{
  "name": "wordle-workshop",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node ./scripts/build-assets.js --minify",
    "watch": "node ./scripts/build-assets.js --watch",
    "dev": "wrangler dev",
    "deploy": "pnpm run build && wrangler deploy --minify",
    "cf-typegen": "wrangler types",
    "check": "biome check",
    "check-summary": "biome check --reporter=summary"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.5",
    "esbuild": "0.25.4",
    "typescript": "^5.9.3",
    "wrangler": "^4.50.0"
  },
  "dependencies": {
    "@alpinejs/csp": "^3.15.2",
    "@alpinejs/persist": "^3.15.2",
    "hono": "^4.10.6"
  }
}
